# 개발 규칙 및 재발 방지 가이드

## 🚨 버그 수정 프로토콜 (필수)

### 1단계: 문제 정확히 파악
- [ ] 사용자에게 **정확한 시나리오** 확인
  - 어떤 화면? 어떤 버튼? 어떤 순서?
- [ ] 스크린샷 요청 (가능한 경우)
- [ ] **실제 클릭한 버튼의 onclick 속성 확인 요청**
  ```javascript
  // Console에서 실행 요청
  document.querySelector('button[onclick*="키워드"]')
  ```

### 2단계: Console 로그 분석
- [ ] 버튼 클릭 시 **실제로 호출되는 함수** 확인
- [ ] 오류 메시지 전체 확인
- [ ] **가정하지 말고 확인하기**

### 3단계: 올바른 파일/함수 찾기
- [ ] Grep으로 함수 정의 위치 검색
- [ ] 여러 파일에 같은 이름 함수가 있는지 확인
- [ ] **엉뚱한 파일 수정하지 않기**

### 4단계: 수정 전 완료 기준 명확화
- [ ] 사용자가 원하는 **정확한 결과** 확인
  - 어디에 무엇이 표시되어야 하는가?
  - UI 위치, 스타일, 동작 방식
- [ ] 아스키 아트나 설명으로 시각화

### 5단계: 수정 후 검증
- [ ] 코드만 보고 "완료"라고 하지 않기
- [ ] 사용자에게 실제 테스트 요청
- [ ] 스크린샷으로 결과 확인

## ❌ 절대 금지 사항

1. **가정하지 말기**
   - "아마 이 함수일 것이다" ❌
   - "피드백 패널 = 전체 평가일 것이다" ❌
   - **→ 반드시 확인하기**

2. **변명하지 말기**
   - "캐시 문제입니다" (확인 안 하고) ❌
   - "입력창이 숨겨져서입니다" (핑계) ❌
   - **→ 진짜 원인 찾기**

3. **여러 곳 동시 수정하지 말기**
   - 한 번에 여러 파일 수정 ❌
   - **→ 한 곳씩 수정하고 테스트**

4. **"나중에 구현" 금지**
   - `// TODO`, `// 구현 예정` 남기고 방치 ❌
   - **→ 즉시 완료하거나 사용자에게 알리기**

## ✅ 올바른 디버깅 순서

```
1. 문제 재현 시나리오 확인
   ↓
2. Console에서 어떤 함수가 호출되는가?
   ↓
3. 그 함수는 어느 파일에 있는가?
   ↓
4. 함수 내부에서 무엇이 실패하는가?
   ↓
5. 수정
   ↓
6. 사용자에게 테스트 요청
```

## 📋 버그 리포트 받을 때 체크리스트

사용자에게 다음 정보를 요청:

- [ ] **정확한 재현 단계** (1, 2, 3...)
- [ ] 어떤 화면인가?
- [ ] 클릭한 버튼의 텍스트
- [ ] **Console 로그 전체**
- [ ] 예상 동작 vs 실제 동작

## 🔍 Common Mistakes (과거 실수)

### Case 1: 첨부파일 미리보기 (2026-01-28)
**문제:** "첨부파일이 안 보인다"
**실수:**
- ❌ `uploadEditAttachmentGeneral()` 수정 (엉뚱한 함수)
- ❌ "전체 평가 탭"만 생각함
- ❌ 실제 문제는 `uploadAttachment()` (인라인 첨삭용)

**교훈:**
- ✅ 먼저 "어떤 버튼을 클릭했는가?" 확인
- ✅ onclick 속성 확인 요청
- ✅ Grep으로 함수 정의 위치 찾기

**소요 시간:** 1시간 이상 낭비

**근본 원인:**
- 함수를 처음 만들 때 토스트만 넣고 `// 구현 예정` 남긴 채 방치
- 사용자가 클릭한 버튼이 무엇인지 확인하지 않음
- Console 로그를 받았지만 제대로 분석하지 않음

## 💡 질문 템플릿

문제 파악 시 이렇게 질문:

```
1. 정확히 어떤 화면에서 문제가 발생하나요?
2. 클릭한 버튼의 텍스트는 무엇인가요?
3. F12 → Console을 열고 다음을 실행해주세요:
   document.querySelector('button').onclick
   (클릭한 버튼에 커서를 놓고 우클릭 → 검사 → Console에서 $0.onclick)
4. 버튼 클릭 시 Console에 출력되는 내용을 전부 복사해주세요
5. 원하시는 결과는 정확히 어떤 모습인가요?
```

## 🎯 완료 기준 예시

### 좋은 예 ✅
"첨부 버튼 클릭 → 파일 선택 → textarea 바로 아래에 다음 형식으로 표시:
```
📎 filename.pdf (1.2 MB) [X]
```
X 버튼 클릭 시 미리보기 제거"

### 나쁜 예 ❌
"첨부파일이 보이게 하기" (어디에? 어떻게?)

## 🔄 코드 작성 원칙

1. **TODO/구현 예정 금지**
   - 함수를 만들면 즉시 완성하기
   - 미완성 상태로 커밋하지 않기

2. **함수 이름 명확히**
   - `uploadAttachment()` - 어디에 쓰이는지 불명확
   - `uploadInlineCommentAttachment()` - 명확함

3. **Console 로그 추가**
   - 모든 주요 함수에 `console.log('[함수명] 호출됨')` 추가
   - 디버깅 시 어떤 함수가 실행되는지 즉시 확인 가능

4. **DOM 요소 확인**
   - `getElementById()` 사용 전에 null 체크
   - 요소가 없으면 명확한 에러 메시지 출력
   ```javascript
   const el = document.getElementById('myId');
   if (!el) {
       console.error('[함수명] #myId 요소를 찾을 수 없음');
       return;
   }
   ```

## 📝 커밋 전 체크리스트

- [ ] TODO/구현 예정 주석이 없는가?
- [ ] 모든 함수가 완성되었는가?
- [ ] Console 로그가 적절히 추가되었는가?
- [ ] 실제 화면에서 테스트했는가?
- [ ] 교수/관리자 화면도 같이 수정했는가? (공통 기능인 경우)

---

**마지막 업데이트:** 2026-01-28
**작성자:** Claude Code (실수로부터 학습)
